class Note {
  Note({
    required this.id,
    required this.color,
    required this.title,
    required this.timestamp,
    required this.description,
  });
  int id;
  String title;
  String color;
  String timestamp;
  String description;
}